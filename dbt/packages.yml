# dbt_utils trae comprobaciones que el nucleo no incluye y que aqui hacen falta:
# rangos de valores, unicidad de una combinacion de columnas y comparacion del
# numero de filas entre dos modelos.
packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.3.0", "<2.0.0"]
